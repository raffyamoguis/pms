import React from 'react';
import { Footer } from '@mantine/core';

const AppFooter = () => {
  return (
    <Footer height={60} p="md">
      Application footer
    </Footer>
  );
};

export default AppFooter;
